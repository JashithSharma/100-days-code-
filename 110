#include <stdio.h>

int main() {
    int n, k, i, j;
    int arr[200];

    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter %d elements:\n", n);
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Enter k: ");
    scanf("%d", &k);

    if(k > n || k <= 0) {
        printf("-1\n");
        return 0;
    }

    for(i = 0; i <= n - k; i++) {
        int max = arr[i];

        for(j = i; j < i + k; j++) {
            if(arr[j] > max) {
                max = arr[j];
            }
        }

        printf("%d ", max);
    }

    printf("\n");
    return 0;
}
